'use strict';

// Global (name)
	// Local (name)
		// Local 	
	// Local 

// let name = 'Pascal';

if (true) {
	// let name = 'Mike';

	if (true) {
		name = 'Jen';
		console.log(name);
	}
	
	console.log(name);
}

if (true) {
	console.log(name);
}